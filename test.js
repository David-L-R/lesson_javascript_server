// user

const user = {
  name: "David",
  age: 35,
  isAdult: false,
};

// DRY Principle
// Don't Repeat Yourself

const { name, age, isAdult } = user;

console.log(name);

// GIT
// Version control
